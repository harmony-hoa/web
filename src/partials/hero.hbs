<section
    class="my-6 bg-repeat-x bg-bottom bg-middle"
    style="background-image: url(assets/houses-bgd.svg); background-size: 300px;">
    <div class="max-w-7xl mx-auto space-y-3 px-4">
        <h1 class="text-4xl sm:text-8xl font-serif mb-12 mt-8">
            HOA
            <span id="words-container" class="hidden">
                <em>Payments</em>
                <em>Social</em>
                <em>Management</em>
                <em>Security</em>
                {{!-- <em>Vendors</em> --}}
                <em>Thrive</em>
            </span>
            <em id="typing-text" class="text-yellow-500 h-10 block sm:inline"></em>
            <span class="block">Platform</span>
        </h1>

        <div class="flex gap-6">
            <div class="space-y-3 text-zinc-400">
                <p>We are building the most comprehensive and innovative community platform
                    focused on community wellbeing and harmony. </p>

                <p>A technology fusion of proptech, fintech, social,
                    blockchain and AI, laser focused to help every community to thrive. </p>

                {{!-- <p>
                    <button class="btn btn-primary">
                        Book Now
                    </button>
                </p> --}}
                <div class="pt-6">
                    <div data-tf-live="01JK455841X7Z7NT4AWKY53GEM"></div>
                </div>

                <div class="h-16"></div>
            </div>

            <div role="presentation" class="relative shrink-0 w-1/3 self-end mt-8 mx-8 hidden sm:block">
                <img src="assets/house-picture.svg" alt="house">
                <img src="assets/balloon.svg"
                    class="w-1/5 absolute animate-randomFloat top-0 right-0"
                    alt="ballon">
            </div>
        </div>


    </div>
</section>


<script src="//embed.typeform.com/next/embed.js"></script>
<script>
(function () {
    const typingText = document.getElementById('typing-text');
    const words = document.getElementById('words-container').getElementsByTagName('em');
    let wordIndex = 0;
    let charIndex = 0;
    let isDeleting = false;

    function typeWord() {
        const currentWord = words[wordIndex].innerText;
        const diff = isDeleting ? -1 : 1;
        typingText.textContent = currentWord.substring(0, charIndex + diff);
        charIndex = charIndex + diff;

        if (!isDeleting && charIndex === currentWord.length) {
            if (wordIndex === words.length - 1) return;
            isDeleting = true;
            setTimeout(typeWord, 1500);
        }
        else if (isDeleting && charIndex === 0) {
            isDeleting = false;
            wordIndex++;
            setTimeout(typeWord, 500);
        }
        else {
            setTimeout(typeWord, isDeleting ? 100 : 200);
        }
    }
    typeWord();
})();
</script>

